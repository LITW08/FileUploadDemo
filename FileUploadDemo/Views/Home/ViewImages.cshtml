@model ViewImagesViewModel

<div class="col-md-8 offset-md-2">
    @foreach (var image in Model.Images)
    {
        <div class="mb-4">
            <img src="/uploads/@image.FileName" style="width: 500px;"/>
            <h3>@image.TimeUploaded.ToLongTimeString()</h3>
        </div>
    }

</div>